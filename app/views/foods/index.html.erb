 <div class = "container">
   <div class = "raw space"> 
   <h2>All Recipe</h2>
    <% @foods.each do |food| %>
      <%= link_to food_path(food.id) do %>
        <%= attachment_image_tag food, :image, size: "200x200", format: 'jpeg', fallback: "no_image.jpg" %> 
      <% end %>
    <% end %>
   </div>
 </div>